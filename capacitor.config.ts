import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ping-pong-ui',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
